<ir_task>
  <system_directive>
    Analyze the conversation context. Do not reply to the user.
    Output only the specified XML format.
    CRITICAL: Your response must begin with "<analysis>" - no preamble, no reasoning, no explanation before the XML.
  </system_directive>

  <instruction>Perform four tasks on the conversation context below in order.</instruction>

  <task id="1" name="entity_extraction">
    <description>Extract entities compatible with spaCy NER categories for a Knowledge Graph.</description>

    <entity_types>
      <type name="PERSON">People, user names (spaCy: PERSON)</type>
      <type name="ORG">Companies, organizations (spaCy: ORG)</type>
      <type name="PRODUCT">Software, tools, languages, protocols (spaCy: PRODUCT)</type>
      <type name="EVENT">Specific named events (spaCy: EVENT)</type>
      <type name="GPE">Geographic locations (spaCy: GPE)</type>
    </entity_types>

    ║⊕║ ENTITY RULES ║⊕║
    <entity_constraint>
      You MUST apply this test to every candidate entity before including it:
      Would a spaCy NER model tag this exact string as PERSON, ORG, PRODUCT, EVENT, or GPE?
      If NO — do not extract it. "Build pipeline", "testing", "Father", "subcortical", "PATCHNOTE" are NOT entities.
      Only extract proper nouns: specific names of people, companies, software, places, or events.
      When no entities pass this test, output "None".
    </entity_constraint>
    ║⊕║ END ENTITY RULES ║⊕║
  </task>

  <task id="2" name="passage_filtering">
    <description>Decide which previously-surfaced stored passages remain relevant.</description>

    <input_format>
      <explanation>Each stored passage shown as:</explanation>
      <example>mem_a1B2c3D4 [●●●○○] - Passage text</example>
      <note>The 5-dot indicator reflects system-calculated importance. Use as signal, not mandate—conversational relevance can override low scores.</note>
    </input_format>

    ║⊗║ PASSAGE RULES ║⊗║
    <rules>
      <rule>Preserve passages providing useful background even if not directly referenced</rule>
      <rule>Drop entries from topics the conversation has moved past</rule>
      <rule>Output ONLY passages that remain relevant, marked with still_relevant="true"</rule>
      <rule>CRUCIAL DETAIL: Copy the full passage ID (including mem_ prefix) exactly. The system matches by ID only.</rule>
    </rules>
    ║⊗║ END PASSAGE RULES ║⊗║
  </task>

  <task id="3" name="query_expansion">
    <description>Perform a metacognitive analysis of the user's intent to generate retrieval-optimized text for embedding similarity search.</description>

    <context>
      <explanation>Stored passages describe beliefs, preferences, decisions, and experiences with specific details.</explanation>
      <stored_passage_example>Dislikes running gas heater in garage — smell, noise, and dry air make workspace feel like construction site rather than lab</stored_passage_example>
      <stored_passage_example>Dog required emergency surgery after swallowing corn cob — $2,200, wiped out emergency fund, did not hesitate on treatment</stored_passage_example>
      <stored_passage_example>Enjoys gutter punk music but prefers tracks with jubilant or positive vibes, such as 'Lisa Leslie' by DijahSB</stored_passage_example>
      <instruction>Your expansion must use words that overlap with how these passages are written.</instruction>
    </context>

    ║⊙║ EXPANSION RULES ║⊙║
    <rules>
      <rule>Dereference pronouns to concrete nouns using conversation context</rule>
      <rule>Include domain context that would appear in stored memories</rule>
      <rule>Write 2-4 semicolon-separated phrases that would match stored memories about this topic</rule>
      <rule>Capture the recent conversational arc with a bias towards the most recent turns</rule>
      <rule>CRITICAL: Never leave this block empty. If the message is short, expand on the current topic or user state.</rule>
    </rules>
    ║⊙║ END EXPANSION RULES ║⊙║

    <examples>
      <example>
        <user_message>This afternoon I'm going to keep coding the CRM for my customer.</user_message>
        <expansion>building custom CRM for window cleaning business; tracking customer retention and repeat scheduling; database design for service area management</expansion>
      </example>
      <example>
        <user_message>I want to do steady escalation. I want to talk to the executive team and remind them their policy is violating federal law.</user_message>
        <expansion>escalation strategy against company policy violation; contacting executive team about federal law breach; pursuing attorney general complaint and legal pressure</expansion>
      </example>
      <example>
        <user_message>How aware are you of all of the different pot shots like what are they</user_message>
        <expansion>criticism and negative feedback received; complaints and objections raised by detractors; concerns mentioned about approach or decisions</expansion>
      </example>
    </examples>
  </task>

  <task id="4" name="complexity_assessment">
    <description>Assess the cognitive complexity of the user's inquiry to calibrate response depth.</description>

    <criteria>
      <straightforward>
        <criterion>Simple factual questions or status updates</criterion>
        <criterion>Casual conversation or emotional check-ins</criterion>
        <criterion>Single-step requests with clear answers</criterion>
        <criterion>Acknowledgments, confirmations, or brief reactions</criterion>
      </straightforward>
      <complex>
        <criterion>Multi-step reasoning or planning required</criterion>
        <criterion>Technical problem-solving or debugging</criterion>
        <criterion>Weighing tradeoffs or making decisions with multiple factors</criterion>
        <criterion>Creative work requiring exploration of possibilities</criterion>
        <criterion>Analysis requiring synthesis of multiple concepts</criterion>
      </complex>
    </criteria>

    ║⊛║ COMPLEXITY RULES ║⊛║
    <rules>
      <rule>Evaluate based on cognitive effort required to respond well, not message length</rule>
      <rule>Output exactly one of: straightforward OR complex</rule>
    </rules>
    ║⊛║ END COMPLEXITY RULES ║⊛║
  </task>

  <output_format>
    <instruction>Start your response immediately with &lt;analysis&gt;. No preamble.</instruction>
    <template><![CDATA[
<analysis>
<entities><named_entity>Name</named_entity></entities>
<relevant_passages>
<passage id="mem_XXXX" still_relevant="true">Passage text</passage>
</relevant_passages>
<query_expansion>phrase1; phrase2; phrase3</query_expansion>
<complexity>straightforward</complexity>
</analysis>]]></template>
    <note>Entities block: one or more named_entity tags, or literal "None". Passages: only still-relevant with exact mem_ IDs.</note>
  </output_format>
</ir_task>
------------------------------------------------------------